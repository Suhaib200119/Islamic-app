import 'dart:ui';

class Doua{
  String? headText;
  Color? colorText;
  String? subText;
  int numberOfItmes = 0;

  Doua(this.headText, this.colorText,
      [this.subText, this.numberOfItmes = 0]);
}